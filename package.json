{
  "name": "peak-map-plotter",
  "version": "0.1.0",
  "description": "Plot-ready topographic line maps. Fork of anvaka/peak-map.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "lidar:list": "node scripts/fetchLidar.mjs --list",
    "lidar:fetch": "node scripts/fetchLidar.mjs",
    "test:watch": "vitest",
    "deploy": "npm run build && wrangler deploy",
    "deploy:dry": "npm run build && wrangler deploy --dry-run"
  },
  "dependencies": {
    "fast-xml-parser": "^5.11.1",
    "geotiff": "^3.0.5",
    "maplibre-gl": "^4.7.1",
    "point-in-polygon": "^1.1.0",
    "tinycolor2": "^1.6.0",
    "vue": "^2.7.16",
    "vue-color": "^2.8.1"
  },
  "devDependencies": {
    "@vitejs/plugin-vue2": "^2.3.3",
    "stylus": "^0.63.0",
    "vite": "^5.4.11",
    "vitest": "^2.1.8",
    "wrangler": "^4.127.1"
  }
}
